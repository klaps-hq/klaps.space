import React from "react";
import SectionLoader from "@/components/ui/section-loader";

const ScreeningsPageLoader: React.FC = () => (
  <SectionLoader label="Ładowanie seansów…" />
);

export default ScreeningsPageLoader;
